#include <string>

using std::string;

namespace manager_consts {
    const string load_profile = "Load Profile";
    const string save_profile = "Save Profile";
    const string default_entry_name = "";
}